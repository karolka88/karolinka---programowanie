#nested loop = A loop within another loop (outer, inner)
#               outer loop:
#                    inner loop:

#for x in range(3):          #3-krotne powtórzenie czegoś
#    for y in range(1, 10):
#        print(y, end=" ")   #liczby w jednej linii oddzielone np. spacją, - , lub \n(osobne linijki)
#    print()                 #new line

#rows = int(input("Enter the number of rows: "))
#columns = int(input("Enter the number of columns: "))
#symbol = input("Enter a symbol to use: ")

#for x in range(rows):               #liczba wierszy
#    for y in range(columns):        #liczba kolumn
#        print(symbol, end = "")     #czym odzielone są symbole
#    print()                         #osobne linijki

#może byc for in for, może byc for in while, moze być while in for, może być while in while